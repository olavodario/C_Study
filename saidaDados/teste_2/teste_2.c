#include <stdio.h>

int main () {
    int x;

    printf("%d", x); //nesse meu primeiro teste ele deu a saida de 200, isso acontece pq ele pega o valor que esta no lixo da mamoria e imprime, pois nao atribuimos valor no 'x'!
    
    return 0;
}